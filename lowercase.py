# function to convert the string to lowercase, using the lower level algorithm. (i.e.) ascii table
# ascii table convertion.


print(ord("a"))
print(ord("A"))

# using the print f function

print(f"The ascii value of A is {ord('A')}")

# ord() and chr() functions can be used to convert the char to ascii and vice versa
